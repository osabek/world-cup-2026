/* Filled in automatically when the Firebase project is provisioned.
   The web config below is PUBLIC by design (Firebase security lives in
   firestore.rules, not in hiding these values). */
window.FIREBASE_CONFIG = null;
